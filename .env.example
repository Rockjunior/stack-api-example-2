# Example environment variables for local development
# Copy this file to `.env` and fill in the values before running the project.
# Do NOT commit your real secrets. `.env` is already in .gitignore.

# Your Supabase project URL (example: https://xyzcompany.supabase.co)
SUPABASE_URL=https://your-project.supabase.co

# Supabase service role key (used by the API for server-side operations)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Supabase anon/public key (used by client-side auth flows)
SUPABASE_ANON_KEY=your_anon_key_here

# (Optional) OpenAI API key if you plan to use OpenAI features
OPENAI_API_KEY=your_openai_api_key_here
